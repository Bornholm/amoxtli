// greet says hello.
function greet(name) {
  return `hello ${name}`;
}

/** A tiny server. */
class Server {
  /** Start the server. */
  start() {
    return true;
  }
}

const handler = (req) => req.body;
